#!/bin/bash

#$1 - имя архива

	if [ ! "$1" ]; then
	echo 'Необходим аргумент-файл архива tar.7z! Выход.'
	exit 1
	fi

NICE=16	#С каким приоритетом, м.б. очень долгим и затратным процессом, в зависимости от размера архива

nice -n $NICE \
	7za x $ZIP7_OPTIONS_ADDON -so $1 | tar --list
