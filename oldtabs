#!/bin/bash

	if [ $1 ]
	then
	sed -i 's/\t/\t\t/g' $1 #Дублируем табы, поспкольку они раньше были половинными
	sed -i 's/    /\t/g' $1 #4 пробела преобразуем в таб
	echo $1
	else
	echo Нету аргументов. Должен передаваться файл для обработки. Завершаю работу.
	exit
	fi


