#!/usr/bin/bash

curl -sS --max-time=30 --fail-early --fail-with-body https://ifconfig.co/json | jq '{ip, country, city, country_iso, country_eu, region_name, region_code, time_zone, asn_org, hostname}'